openapi: 3.1.0
info:
  title: Vision Center Public API Contract
  version: 1.0.0
paths:
  /api/public/branches/current:
    get:
      summary: Return the active branch for the current session
      responses:
        '200': { description: Active branch }
        '401': { description: Authentication required }
